import printFunctions

warning  = "convertAntPosFile is now included by default in the system and \n"
warning += "can be used directly without prefixing this module name before it."
warning += "\nType help( convertAntPosFile ) for more information or just type "
warning += "\nconvertAntPosFile( filename ) to use the function." 

printFunctions.printWarning( warning )
